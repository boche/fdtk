# 开发守则

## 核心原则

每次开发新功能后，必须同时验证两件事：
- 新功能可以正常使用。
- 原有功能没有被改崩。

不能只因为“新功能能点开”就结束。只要改动影响既有流程，就必须做回归验证。

## Karpathy 风格开发纪律

本项目采用一组精简开发纪律，整理自 forrestchang/andrej-karpathy-skills 的中文说明：https://github.com/forrestchang/andrej-karpathy-skills/blob/main/README.zh.md

本项目不照搬插件安装流程，只吸收可执行的工程规则：
- 编码前思考：先说明假设、影响范围和取舍；遇到歧义时不要默默猜。
- 简洁优先：优先最少代码和最少抽象，不为暂未需要的功能预留复杂架构。
- 精准修改：只碰本次任务必须碰的文件；不顺手重构无关代码。
- 目标驱动执行：每个非平凡任务都要有可验证成功标准，并循环修正直到通过。

## 开发流程

1. 明确本次改动影响的模块、页面和数据结构。
2. 写清楚成功标准，例如“武将技能会改变征税收益和征兵副作用”。
3. 完成功能实现，保持实现轻量，不引入新的框架依赖。
4. 验证新功能主路径。
5. 运行最小回归集，确认旧功能仍可用。
6. 如果发现回归问题，先修复回归，再继续新增功能。
7. 在更新记录或提交说明中写清楚改了什么、测了什么、还有什么没测。

## 游戏设计约束

藩镇图开是极简单机网页游戏，不追求完整复刻《三国志10》，但数值设计应参考其核心思路：人物、城市、人口、兵力、内政和战争要互相影响。

必须遵守：
- 武将技能不能只做描述，至少要影响一个实际公式或可见结果。
- 征税必须有副作用：税负上升、人口外流、治安或民心下降。
- 征兵必须遵守人口与兵力转换：新增兵力来自人口，不能凭空增加。
- 人口增长必须缓慢，不能靠月度恢复快速膨胀。
- 任命和职责必须有意义：理政、练兵、守备、招揽至少要影响对应行动的候选人或结果。
- 战斗必须读取主将、守将、兵种、训练、士气、城防、地形和军事技能。
- AI 使用同一套约束，不能绕过人口、税负、兵役和技能系统。

## 新功能验证

每个新功能至少检查：
- 能进入对应界面或流程。
- 关键操作可以执行。
- 结果符合预期公式或日志说明。
- 至少覆盖一个正常路径。
- 至少覆盖一个边界或失败路径。

对数值功能还必须检查：
- 修改前后的关键数值有可解释变化。
- 日志或反馈能说明为什么发生变化。
- AI 和玩家路径没有使用两套互相矛盾的规则。

## 最小回归集

每次迭代后，至少验证以下内容：
- 能打开 `index.html`。
- 能新开一局。
- 能选择势力进入主界面。
- 能切换内政、军事、武将面板。
- 能执行至少一个内政命令。
- 能执行至少一个军政命令或结束本月。
- 中文显示不乱码。
- 主界面没有明显布局崩坏。
- 控制台没有新的致命报错。

## 本项目特别要求

凡是改动以下内容之一，都必须追加针对性回归：
- 地图与主界面布局。
- 武将面板、头像、简介与技能。
- 人口、税负、兵役、月度恢复。
- 内政命令公式。
- 战争结算和 AI 出兵。
- 存档读档。
- 中文文案与编码。

## 验收标准

只有同时满足以下条件，才算本次开发完成：
- 新功能可用。
- 旧功能未崩。
- 至少完成一轮手动或脚本化验证。
- 数值改动有日志、反馈或代码公式可追踪。
- 结果已写入更新记录或提交说明。
