# 藩镇图开

一个五代十国背景的极简中文版单机网页游戏原型。

## 运行方式

由于当前环境缺少 Node/npm，这个版本采用原生 HTML/CSS/JavaScript 实现。

可直接打开：
- C:\Users\aimbo\Documents\fdtk\index.html

如果浏览器限制本地脚本加载，也可以用任意静态文件服务器指向该目录。


## 部署到 GitHub Pages（xx.github.io 直接游玩）

本项目是纯静态页面（HTML/CSS/JS），可以直接部署到 GitHub Pages。

1. 把仓库推到 GitHub（建议默认分支为 `main`）。
2. 打开仓库 `Settings -> Pages`，将 Source 设为 **GitHub Actions**。
3. 本仓库已提供工作流：`.github/workflows/deploy-pages.yml`，每次推送到 `main` 会自动发布。

发布地址规则：
- 用户/组织主页仓库（仓库名必须与用户名一致，即 `<用户名>.github.io`）：`https://<用户名>.github.io/`
- 普通仓库：`https://<用户名>.github.io/<仓库名>/`

例如（假设你的 GitHub 用户名是 `alice`）：
- 仓库名 `alice.github.io` -> `https://alice.github.io/`
- 仓库名 `fdtk` -> `https://alice.github.io/fdtk/`

> 说明：代码里使用了相对路径（如 `./src/...`），兼容上述两种地址形式。

## 当前内容

- 1 个剧本：923：后唐开国
- 10 座城
- 5 个势力
- 50 名人物
- 月度经营、任命、招揽、自动战斗、停战、存档

## 开发约定

- 开发守则见 [DEVELOPMENT.md](./DEVELOPMENT.md)
- 每次新增功能后，必须同时验证“新功能可用”和“旧功能未崩”

## 目录

- src/data：剧本与中文文案
- src/core：规则与存档
- src/ui：渲染
- src/main.js：入口

## 更新记录

### 2026-03-08

- 重做桌面主界面布局，尽量避免页面级上下滚动，保留左侧命令、中部城内视图、右侧信息栏的整体结构。
- 加入全屏切换、轻量音效、背景音乐与基础战争动效，增强月度推进和出征反馈。
- 强化地图表现，增加底图、节点标注、行军线和战事提示，小地图可直接识别各城市。
- 新增军师月报、帮助抽屉和悬浮说明，降低新手理解门槛。
- 将命令整理为二级菜单：内政、军事、武将，简化主操作区。
- 补充武将简介、职责分配、待任官员与在野武将信息展示。
- 让武将能力开始影响内政和征兵等核心环节，并收紧征税、征兵的副作用与可用条件。
- 修复征兵后人口与兵力不守恒的问题，补入低忠诚/低治安下的叛乱逻辑。
- 提升 AI 侵略性，敌对势力会更主动进攻。
- 修复部分回合结束与开局渲染问题，补做浏览器回归检查。
- 修复本轮出现的中文乱码问题，改为更稳的 ASCII-safe UI 脚本输出。
- 补上头像悬浮生平提示，并为头像失败场景加入内置 SVG 人像兜底。
